# AgentChat 设计文档包

类 claude.ai 的自定义 Agent 聊天应用，后端对接 Claude Agent SDK (Python, 锁定 0.2.110)。
打包日期：2026-07-06（v2，含 10 号交互设计文档与对应 UI 更新）

## 阅读顺序

| 文档 | 内容 |
|---|---|
| 01-总体架构设计 | 分层架构、三存储职责边界、SSE 通信协议、部署形态 |
| 02-数据模型设计 | 全部表结构、SQLite/MySQL 双兼容规范、迁移方案 |
| 03-核心功能实现 | 对话流/停止/编辑分支/斜杠命令/Subagent/MCP/文件/Artifacts |
| 04-安全与扩展 | 隔离→权限→Hook→审批→审计五层纵深、扩展点设计 |
| 05-API规范 | REST + SSE 完整接口定义 |
| 06-工程实施指南 | 版本基线、SDK 关键语义、核心模块代码、里程碑与验收标准、常见坑 |
| 07-产品化方案 | 可嵌入(iframe/WebComponent/React) + 独立运行、身份联合、打包分发 |
| 08-监控审计与场景化启动 | 可观测性体系；外部跳转→预置工作区→受限探索→重置/复制 |
| 09-Skill共享与组协作 | Skill 三层管理、组内共享采纳、只读会话、设置治理、权限链最终版 |
| 10-输入与消息交互设计 | 附件暂存(发送才生效)、粘贴转缩略图、消息操作栏、＋两级菜单 |

## 前端

AgentChatUI.jsx — React 单文件，可直接在支持 JSX 的环境预览。
包含：对话流式/停止、工具卡片、审批、斜杠命令面板、＋附件两级菜单、附件暂存与
粘贴缩略图、消息操作栏(复制/下载/点赞/点踩/重新生成)、工作区文件树(预览/下载)、
场景会话(重置/副本)、组空间、权限管理控制台(含审计视图)、设置治理锁定。
所有后端接线点集中在文件顶部注释与 mockApi 对象。

## 重要提醒

- 涉及 Agent SDK 具体参数名的内容以 claude-agent-sdk==0.2.87 为基准，升级前过契约测试。
- python版本使用 3.12
- allowed_tools 是自动放行白名单而非工具裁剪，禁用工具必须用 disallowed_tools。
- hooks 与自定义工具需 ClaudeSDKClient，简单 query() 不支持。
- 对外提供服务必须走 API key 认证计费。
